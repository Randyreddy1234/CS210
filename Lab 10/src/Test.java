import java.util.*;

class test {

    public String getMove(boolean north, boolean south, boolean east, boolean west) {
        return"";
    }

}

// representing the maze in a adjaceny matrix, each co-ord is a node and should add an
// edge between two adjecent points if theres no wall
class MazeGraph{
    private int size = 20;
    private boolean [][] adjMatrix = new boolean[size][size];

}
